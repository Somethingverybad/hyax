.env
# Резервная копия старой iOS-платформы (пересоздана заново)
sux-chat-app/ios-old/
.DS_Store

# Ключи Firebase и прочие секреты (не коммитить!)
secrets/

# Firebase-конфиг iOS: нужен сборке локально, но репа публичная —
# наружу не выкладываем. Реквизиты описаны в secrets/README.md
sux-chat-app/ios/App/App/GoogleService-Info.plist
